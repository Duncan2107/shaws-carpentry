# Full-size original photos (~121 MB). Kept locally only — never deployed,
# and deliberately not committed so the repo stays small.
# BACK THESE UP SEPARATELY: they are the only high-resolution copies.
/originals/

# Local tooling and editor/OS noise
/.claude/
.DS_Store
Thumbs.db
desktop.ini

# Node (used from Phase 2 onward for Wrangler)
node_modules/
.wrangler/
.dev.vars

# Generated by db/set-admin-password.js. Holds a password hash, not a
# password, but it is still an account credential: apply it and delete it.
/db/admin-password.sql
